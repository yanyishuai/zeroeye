openapi: 3.1.0
info:
  title: Empty Enum Fixture
  version: 1.0.0
paths:
  /orders:
    get:
      operationId: listOrders
      responses:
        "200":
          description: ok
components:
  schemas:
    EmptyEnum:
      type: string
      enum: []

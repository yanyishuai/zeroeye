openapi: 3.1.0
info:
  title: Unsupported Enum Fixture
  version: 1.0.0
paths:
  /orders:
    get:
      operationId: listOrders
      responses:
        "200":
          description: ok
components:
  schemas:
    UnsupportedEnum:
      type: object
      enum:
        - kind: market

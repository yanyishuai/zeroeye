openapi: 3.1.0
info:
  title: Enum Validation Fixture
  version: 1.0.0
paths:
  /orders:
    get:
      operationId: listOrders
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [open, closed, pending]
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  side:
                    type: string
                    enum: [buy, sell]
components:
  schemas:
    Order:
      type: object
      properties:
        kind:
          type: string
          enum: [limit, market]

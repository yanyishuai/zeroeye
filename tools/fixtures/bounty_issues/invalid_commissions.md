**Bounty:** $25 (LT)

Acceptance criteria:
- Add a validator command

Commissions:

You can earn an extra $5 for every bounty issue you create on your fork, provided you keep up with submissions.

Required validation:
- Include the generated diagnostic `.logd` artifact from `diagnostic/build-XXX.logd`; this must be a real diagnostic log and must not be `diagnostic/build-00000000.logd`
